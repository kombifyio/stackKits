# GoReleaser configuration for StackKits CLI
# See: https://goreleaser.com/customization/

version: 2
project_name: stackkits

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: stackkit
    main: ./cmd/stackkit
    binary: stackkit
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X main.Version={{ .Version }}
      - -X main.GitCommit={{ .Commit }}
      - -X main.BuildDate={{ .Date }}

archives:
  - id: default
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    files:
      - README.md
      - LICENSE
      - docs/CLI.md

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
      - "^style:"
      - Merge pull request
      - Merge branch
  groups:
    - title: "Breaking Changes"
      regexp: '^.*?!:.+$'
      order: 0
    - title: "Features"
      regexp: '^.*?feat(\(.+\))?:.+$'
      order: 1
    - title: "Bug Fixes"
      regexp: '^.*?fix(\(.+\))?:.+$'
      order: 2
    - title: "Other"
      order: 999

release:
  github:
    owner: kombifyio
    name: stackKits
  draft: false
  prerelease: auto
  name_template: "v{{ .Version }}"
  header: |
    ## StackKits CLI v{{ .Version }}

    ### Quick Install (Linux/macOS)

    ```bash
    curl -sSL https://raw.githubusercontent.com/kombifyio/stackKits/main/install.sh | sh
    ```

    Or download the archive for your platform below and place `stackkit` in your PATH.
  footer: |
    **Full Changelog**: https://github.com/kombifyio/stackKits/compare/{{ .PreviousTag }}...{{ .Tag }}
